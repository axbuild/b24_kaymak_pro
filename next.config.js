/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    assetPrefix: '',//'/b24_kaymak_pro',
    images: {
      unoptimized: true,
    }
};

module.exports = nextConfig;
